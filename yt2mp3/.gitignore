__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# cli.py's actual default output dir is ~/yt2mp3/pesme, outside the repo,
# so this only guards a relative "-o pesme" run from inside a checkout --
# downloaded audio is not source either way.
pesme/
